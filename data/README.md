# Data Directory

Place the competition dataset files here before running the solution:

- `train.csv`
- `test.csv`
- `sample_submission.csv`

The repository includes `sample_submission.csv` and the final generated `outputs/submission.csv`. The full train/test files are excluded from version control by default.
